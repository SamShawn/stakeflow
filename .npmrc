# 使用严格的对等依赖
strict-peer-dependencies=false

# 自动安装 peer deps
auto-install-peers=true

# 不提升依赖（保持 node_modules 干净）
shamefully-hoist=false

# 锁文件版本
lockfile-version=9.0

# 保存精确版本
save-exact=false

# 国内镜像（可选，按需启用）
# registry=https://registry.npmmirror.com
